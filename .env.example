# No API keys are required at repository initialization.
# Future agent adapters should use existing authenticated Codex or Claude Code installations.

JARVIS_DATA_DIR=./data
JARVIS_DEFAULT_AGENT=codex
